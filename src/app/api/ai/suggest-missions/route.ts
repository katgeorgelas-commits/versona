import { notYetImplemented } from "@/lib/api/stub";

/**
 * POST /api/ai/suggest-missions
 * Recommends missions to join from profile data (PRD §3.6). Suggestions, never
 * defaults; dismissed suggestions are remembered. Built in Feature 3.
 */
export function POST() {
  return notYetImplemented("Mission suggestions", "§3.6");
}
