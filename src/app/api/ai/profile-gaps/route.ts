import { notYetImplemented } from "@/lib/api/stub";

/**
 * POST /api/ai/profile-gaps
 * Notices incomplete profile sections and returns gentle, dismissible nudges
 * (PRD §3.6). Suggestions, never auto-fills. Built in Feature 2.
 */
export function POST() {
  return notYetImplemented("Profile gap nudges", "§3.6");
}
