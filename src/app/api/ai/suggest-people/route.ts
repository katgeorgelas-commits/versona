import { notYetImplemented } from "@/lib/api/stub";

/**
 * POST /api/ai/suggest-people
 * "People like you" — surfaces profiles aligned on values + work style, NOT
 * keyword matching (PRD §3.6). Built in Feature 5.
 */
export function POST() {
  return notYetImplemented("People suggestions", "§3.6");
}
