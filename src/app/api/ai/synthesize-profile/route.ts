import { notYetImplemented } from "@/lib/api/stub";

/**
 * POST /api/ai/synthesize-profile
 * Generates the 2–3 sentence identity snapshot + structured fields from the
 * onboarding transcript (PRD §3.6). Output is always presented for user review
 * and edit — never written to public copy automatically. Built in Feature 1/2.
 */
export function POST() {
  return notYetImplemented("Profile synthesis", "§3.6");
}
