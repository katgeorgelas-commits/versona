import { notYetImplemented } from "@/lib/api/stub";

/**
 * POST /api/ai/weekly-prompt
 * Generates or selects a mission's rotating weekly discussion prompt (PRD §3.3,
 * §3.6). Admin can override. Built in Feature 3 (admin override in Feature 8).
 */
export function POST() {
  return notYetImplemented("Weekly prompt generation", "§3.3");
}
